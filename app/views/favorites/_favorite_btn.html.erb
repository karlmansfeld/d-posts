<% if current_user.favpost?(micropost) %>
    <%= render 'favorites/destroy', micropost: micropost %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
<% else %>
    <%= render 'favorites/create', micropost: micropost %>
    　<%= hidden_field_tag :micropost_id, micropost.id %>
<% end %>
